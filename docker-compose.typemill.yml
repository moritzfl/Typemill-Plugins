services:
  typemill:
    build:
      # Local Dockerfile mirrors upstream Typemill v2.23.0 and adds the PHP zip extension.
      context: ./docker/typemill
      args:
        TYPEMILL_VERSION: v2.23.0
    # Chromium (browser tests) crashes with SIGTRAP under Docker's default
    # seccomp profile; this is a local test container only.
    security_opt:
      - seccomp=unconfined
    ports:
      - "8080:80"
    volumes:
      - ./plugins:/var/www/html/plugins
      - ./themes:/var/www/html/themes
      # Outside the webroot: a mount under /var/www/html would shadow any
      # content page named "tests" and expose test files over HTTP.
      - ./tests:/var/www/tests
      - ./.docker/typemill/settings:/var/www/html/settings
      - ./.docker/typemill/content:/var/www/html/content
      - ./.docker/typemill/data:/var/www/html/data
      - ./.docker/typemill/media:/var/www/html/media
      - ./.docker/typemill/cache:/var/www/html/cache
